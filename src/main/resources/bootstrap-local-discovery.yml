spring:
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: mssc-config-server
      username: user
      password: password

eureka:
  client:
    service-url:
      defaultZone: http://user:password@localhost:8761/eureka